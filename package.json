{
  "name": "omniscript",
  "version": "0.1.0",
  "bin": {
    "omni": "dist/cli.js"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "typescript": "^5.0.0",
    "antlr4": "^4.13.1"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/commander": "^2.12.2",
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  },
  "scripts": {
    "build": "tsc && ( [ -f dist/cli.js ] && chmod +x dist/cli.js || echo 'dist/cli.js not found' )",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write ."
  }
}
